class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        #Two Pointer Approach:
        if not nums:
            return False
        p=0
        for i in range(0,len(nums)):
            if nums[i]!=val:
                nums[p]=nums[i]
                p+=1
        return p
