class Solution:
    def maxLengthBetweenEqualCharacters(self, s: str) -> int:
        maxi=-1
        count={}
        for index,value in enumerate(s):
            if value in count:
                maxi=max(maxi,index-count[value]-1)
            else:
                count[value]=index
        return maxi        
