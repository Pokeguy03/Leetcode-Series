class Solution:
    def minimumSteps(self, s: str) -> int:
        # s=list(s)
        # l=0
        # r=1
        # count=0
        # while r<len(s):
        #     if s[l]=='1' and s[r]=='0':
        #         s[l],s[r]=s[r],s[l]
        #         count+=1
        #         if l>0:
        #             l-=1
        #             r-=1
        #         else:
        #             l+=1
        #             r+=1
        #     else:
        #         l+=1
        #         r+=1
        # return count
        steps=0
        count=0
        for i in s:
            if i=='1':
                count+=1
            else:
                steps+=count
        return steps

        
