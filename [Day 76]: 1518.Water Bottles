class Solution:
    def numWaterBottles(self, numBottles: int, numExchange: int) -> int:
        count=numBottles
        
        while numBottles>=numExchange:
            refill=numBottles//numExchange
            count+=refill
            numBottles=refill+(numBottles%numExchange)
        return count
        
