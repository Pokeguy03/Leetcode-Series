class Solution:
    def isPalindrome(self, s: str) -> bool:
        res=''.join(re.findall(r'[A-Za-z0-9]',s))
        palindrome=res.lower()
        if palindrome==palindrome[::-1]:
            return True
        else:
            return False
