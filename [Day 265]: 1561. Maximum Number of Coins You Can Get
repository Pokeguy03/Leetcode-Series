class Solution:
    def maxCoins(self, piles: List[int]) -> int:
        piles.sort()
        n=len(piles)//3
        res=0
        right=len(piles)-2
        for i in range(n):
            res+=piles[right]
            right-=2
        return res

        
