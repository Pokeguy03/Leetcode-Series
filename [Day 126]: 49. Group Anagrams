class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        li=defaultdict(list)
        for i in strs:
            sorte="".join(sorted(i))
            li[sorte].append(i)
        print(li)
        res=list(li.values())
        return res


        
