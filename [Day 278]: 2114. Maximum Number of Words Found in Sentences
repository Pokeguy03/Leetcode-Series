class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        maxi=0
        for i in sentences:
            res=i.split( )
            maxi=max(len(res),maxi)
        return maxi
