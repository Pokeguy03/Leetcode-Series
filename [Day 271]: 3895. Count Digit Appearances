class Solution:
    def countDigitOccurrences(self, nums: list[int], digit: int) -> int:
        st=""
        for i in nums:
            st+=str(i)
        ct=0
        for i in st:
            if int(i)==digit:
                ct+=1
        return ct

        
