class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        if not nums:
            return false
        b={}
        for i in nums:
            if i in b:
                b[i]+=1
            else:
                b[i]=1
        for key,val in b.items():
            if val==1:
                return key
