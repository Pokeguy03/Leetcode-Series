class Solution:
    def subarraySum(self, nums: List[int]) -> int:
        st=0
        total=0
        for i in range(len(nums)):
            st=max(0,i-nums[i])
            total+=sum(nums[st:i+1])
        return total

        
