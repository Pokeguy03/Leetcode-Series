class Solution:
    def maximumProduct(self, nums: List[int]) -> int:
      
        n=len(nums)
        s=sorted(nums)
        a=s[-1]*s[0]*s[1]
        b=s[-1]*s[-2]*s[-3]
        if a>b:
            return a
        else:
            return b
