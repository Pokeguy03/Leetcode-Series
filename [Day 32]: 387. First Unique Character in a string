from collections import Counter
class Solution:
    def firstUniqChar(self, s: str) -> int:
        li=Counter(s)
        for i in range(len(s)):
            if li[s[i]]==1:
                return i
                break
           
        return -1
        
