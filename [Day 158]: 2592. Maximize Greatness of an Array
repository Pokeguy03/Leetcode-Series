class Solution:
    def maximizeGreatness(self, nums: List[int]) -> int:
        nums.sort()
        perm=0
        i=0
        j=0
        while j<len(nums):
            if nums[j]>nums[i]:
                perm+=1
                i+=1
            j+=1
        return perm
