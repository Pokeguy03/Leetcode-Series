class Solution:
    def mirrorDistance(self, n: int) -> int:
        num=""
        temp=n
        while temp>0:
            num1=temp%10
            temp=temp//10
            num+=str(num1)
        return abs(n-int(num))
