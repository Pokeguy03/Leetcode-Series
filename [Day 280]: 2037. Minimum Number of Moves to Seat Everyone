class Solution:
    def minMovesToSeat(self, seats: List[int], students: List[int]) -> int:
        seats.sort()
        students.sort()
        i=0
        s=0
        while i<len(seats):
            s+=abs(students[i]-seats[i])
            i+=1
        return s
        
