class Solution:
    def majorityElement(self, nums: List[int]) -> List[int]:
        n=len(nums)
        li=[]
        count=Counter(nums)
        for i,v in count.items():
            if v>(n//3):
                li.append(i)
        return li

        
