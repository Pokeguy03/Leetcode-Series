class Solution:
    def minOperations(self, boxes: str) -> List[int]:
        res=[]
        n=len(boxes)
        for i in range(n):
            move=0
            for j in range(n):
                if boxes[j]=="1":
                    move+=abs(i-j)
            res.append(move)
        return res

        
