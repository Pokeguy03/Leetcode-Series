class Solution:
    def sumOfGoodIntegers(self, n: int, k: int) -> int:
        ans=0
        for i in range(max(n-k,1),n+k+1):
            if(n & i)==0:
                ans+=i
        return ans
        
