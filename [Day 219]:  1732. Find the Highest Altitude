class Solution:
    def largestAltitude(self, gain: List[int]) -> int:
        res=[0]
        summ=0
        for i in gain:
            summ+=i
            res.append(summ)
        return max(res)
        
